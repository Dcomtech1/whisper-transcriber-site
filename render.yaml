services:
  - type: web
    name: nova-transcribe
    runtime: docker
    rootDir: .
    plan: free   # stays on Render free plan
    envVars:
      - key: WHISPER_MODEL
        value: tiny   # default model, lightweight for free tier
